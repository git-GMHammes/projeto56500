version: "3.8"

services:
  web:
    image: nginx:alpine
    container_name: camera_web
    ports:
      - "56500:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
